<UserControl x:Class="EtwPilot.View.LiveSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Fluent="urn:fluent-ribbon"
             xmlns:controls="clr-namespace:EtwPilot.Controls"
             xmlns:vm="clr-namespace:EtwPilot.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Name="LiveSessionControl"
             DataContext="{Binding Source={x:Static vm:GlobalStateViewModel.Instance},Path=g_SessionViewModel.CurrentLiveSession,ValidatesOnNotifyDataErrors=False}">
    <TabControl Name="LiveSessionProviderDataTabControl"
                ItemsSource="{Binding Tabs}"
                LostFocus="LiveSessionProviderDataTabControl_LostFocus"
                SelectionChanged="LiveSessionProviderDataTabControl_SelectionChanged"
                SelectedIndex="{Binding SelectedProviderIndex}">
        <TabControl.ContentTemplate>
            <DataTemplate DataType="{x:Type vm:ProviderTraceData}">
                <controls:LiveSessionGrid/>
            </DataTemplate>
        </TabControl.ContentTemplate>
    </TabControl>
</UserControl>